<Window x:Class="Focus.Views.ThemeEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Theme editor"
        Height="640" Width="480"
        MinHeight="480" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgAppBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <DockPanel Margin="18">
        <DockPanel DockPanel.Dock="Bottom" Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reset" Style="{DynamicResource FocusButton}" Width="88" Margin="0,0,8,0" Click="Reset_Click"/>
                <Button Content="Apply" Style="{DynamicResource FocusButton}" Width="88" Margin="0,0,8,0" Click="Apply_Click"/>
                <Button Content="Save" Style="{DynamicResource AccentButton}" Width="88" Margin="0,0,8,0" IsDefault="True" Click="Save_Click"/>
                <Button Content="Close" Style="{DynamicResource FocusButton}" Width="88" IsCancel="True" Click="Close_Click"/>
            </StackPanel>
        </DockPanel>

        <StackPanel>
            <TextBlock Text="Theme name" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding ThemeName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource FocusTextBox}" Margin="0,0,0,12"/>

            <TextBlock Text="Colors (hex)" Style="{DynamicResource SectionHeader}" Margin="0,0,0,8"/>
            <ListBox ItemsSource="{Binding Colors}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     Height="460">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding Hex, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource FocusTextBox}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </DockPanel>
</Window>
